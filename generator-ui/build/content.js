// import { Card } from "antd";
// import { createInjectableComponent, injectComponent } from "inject-react-anywhere";

// const inject = async () => {
//   const InjectableCard = createInjectableComponent(Card);

//   const controller = await injectComponent(InjectableCard, {
//       name: 'Oleh',
//   });

//   document.body.append(controller.shadowHost);
// };

// inject();